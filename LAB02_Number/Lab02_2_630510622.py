# 630510622
# ธนกฤต สมุทร (Thanakrit Samut)
#section 002
#Lab02_2
height_m = float(input("Input height (m): "))# Get the height(m) from the keyboard store in the height_m variable.
weight_kg = float(input("Input weight (kg): "))# Get the weight(kg) from the keyboard store in the weight_kg variable.
BMI = weight_kg / (height_m*height_m)#Convert the give height and weight to BMI
print("BMI is %.4f" %BMI)#Show result that can be convert in 4 decimal places.